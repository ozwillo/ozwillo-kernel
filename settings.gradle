rootProject.name = 'oasis'

include 'log4j2-logstash'

include 'oasis-model'
include 'oasis-webapp'
include 'oasis-dist'

rootProject.children.each { project ->
  project.buildFileName = project.name + '.gradle' - ~'^oasis-'
}
